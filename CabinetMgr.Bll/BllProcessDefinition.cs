using CabinetMgr.Dal;
//using Domain.ShangHaiMeasure.Domain;

namespace CabinetMgr.Bll
{
    public class BllProcessDefinition
    {
        /*
        public static ProcessDefinition GetProcessDefinition(string processId)
        {
            if (string.IsNullOrEmpty(processId)) return null;
            return DalProcessDefinition.GetProcessDefinition(processId);
        }

        public static int SetProcessDefinitionImage(string processId, byte[] image)
        {
            if (string.IsNullOrEmpty(processId)) return -100;
            return DalProcessDefinition.SetProcessDefinitionImage(processId, image);
        }*/
    }
}

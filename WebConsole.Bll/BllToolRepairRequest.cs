using System;
using System.Collections;
using System.Collections.Generic;
using Utilities.DbHelper;
using WebConsole.Dal;

namespace WebConsole.Bll
{
    public class BllToolRepairRequest
    {
        //public static int GetRequestCount(DateTime timeStart, DateTime timeEnd)
        //{
        //    return DalToolRepairRequest.GetRequestCount(timeStart, timeEnd);
        //}

        //public static IList SearchRequestRecord(DateTime timeStart, DateTime timeEnd, List<HibernateHelper.OrderEntity> extOrder,
        //    int startIndex, int pageSize)
        //{
        //    return DalToolRepairRequest.SearchRequestRecord(timeStart, timeEnd, extOrder, startIndex, pageSize);
        //}
    }
}
